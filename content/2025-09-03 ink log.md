---
title: 2025-09-03 ink log
publish: "true"
---
Today I finally got to a point in the project where I am able to send messages and receive responses from the LLM, I can continue the interaction by sending more messages and the LLM continues responding.

(Images)

### Developing the event machine

The next step is developing a pattern for handling events that is **readable and easy to extend**. My initial idea is each widget of the application handling its own events, the only issue is that the widget must know that it is selected and it must block other events from being triggered simultaneously.

Example:

Pressing **(Q)** inside the application quits the program, but if the user clicks on the textarea and then presses **(Q)**, the application should register that letter in the textarea instead of quitting the application.

### Mouse events

I need to be able to check exactly where a mouse click happens so I can select what is underneath the mouse cursor, I still don't know how to do that, I'll let you know once I discover.